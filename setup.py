from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The forecasting branch of the VGP project, used to predict future road congestion in différent city.',
    author='xiaohua-redlab',
    license='MIT',
)
